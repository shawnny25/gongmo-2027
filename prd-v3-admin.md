# 글로벌 파트너십 공모 접수 — 설계도 v3 · 어드민

> 이 문서는 그대로 돌리지 않습니다.
> 클로드가 아래 "아직 정하지 못한 것" 을 하나씩 물어보고, 답을 채운 다음에 만듭니다.

## 왜 어드민이 필요한가
지원서가 Supabase에 쌓이는데, 지금은 보려면 Supabase 화면을 직접 열어야 한다.
그리고 **나 말고 다른 사람도** 지원서를 보고 상태를 바꾸게 될 거라서, 비밀번호 잠금이 있는 별도 화면이 필요하다.

## 추천 — C · 신청 확인형

남이 넣은 지원서가 쌓이는 페이지이므로, "들어온 걸 보고 상태를 바꾸는" 어드민이 1순위다.

| | |
|---|---|
| 누가 쓰나 | 나 + 공모 운영을 같이 하는 사람 (누구인지는 아래 질문 5) |
| 무엇을 바꾸나 | 지원서마다 `status`(상태) 칸 하나. 그 외 지원서 내용은 **읽기만** |
| 못 바꾸게 할 것 | 지원자가 쓴 내용(이름·기관명·예산·소개·링크) · 레이아웃 · 색 · 페이지 구조 · 칸 이름 |
| 들어가는 법 | 비밀번호 하나. 회원가입·로그인은 만들지 않는다 |

**여기에 얹으면 좋은 것** — D · 스위치형. "지원 받기 열림/닫힘" 토글 하나. 마감 후 폼을 닫을 때 쓴다. (v2 문서 "앞으로 정해야 할 것 2번"과 연결)

### 바꿀 칸 / 잠글 칸
| 열어둘 것 | 잠글 것 |
|---|---|
| 지원서 `status` (상태) | 지원서의 나머지 모든 칸 |
| 지원 받기 열림/닫힘 스위치 | 공모 이름·기간·소개 문구 (매년 재사용이 정해지면 그때 연다) |

### 표에 추가할 칸
| 칸 이름 | 무엇 | 비고 |
|---|---|---|
| `applications.status` | 지원서 상태 | 단계는 아래 질문 1에서 정한다. 기본값은 "접수" |
| `settings.is_open` | 지원 받기 열림/닫힘 | 참/거짓 하나 |

## 화면 초안
| # | 화면 | 무엇이 보이나 | 무엇을 할 수 있나 |
|---|---|---|---|
| 0 | 잠금 | 비밀번호 입력칸 | 비밀번호를 넣고 들어간다 |
| 1 | 지원서 목록 | 표: 접수 시각 · 이름 · 기관명 · 트랙 · 예산 · 상태. 최신 순 | 행을 눌러 상세로 간다. 상단에 "지원 받기" 스위치 |
| 2 | 지원서 상세 | 8개 칸 전부. 사업계획서 링크는 새 창으로 열림 | 상태를 바꾸고 저장한다 |

> 어드민 화면은 예쁘지 않아도 됩니다. 표와 입력칸이면 끝.

---

## ★ 아직 정하지 못한 것

> **클로드에게: 아래를 하나씩 물어보고 답을 받아 이 문서를 채운 뒤에 만들어주세요.**
> 한 번에 하나씩 묻습니다.

1. **상태를 몇 단계로 나눌까요?**
   - 왜 물어보나: `status` 칸에 들어갈 값과 상세 화면의 선택지가 이걸로 정해진다
   - 보기: (A) 접수 / 완료 (B) 접수 / 확인 / 완료 (C) 접수 / 서류 통과 / 선정 / 탈락 (D) 직접 적기
   - 답: ______

2. **지원서를 엑셀로 내려받을 수 있어야 하나요?**
   - 왜 물어보나: 심사를 밖에서 한다면 내려받기 버튼이 필수다. 화면으로만 볼 거면 안 만든다
   - 보기: (A) 화면으로만 본다 (B) 엑셀(CSV)로 내려받기 버튼이 있어야 한다
   - 답: ______

3. **지원서의 개인정보는 얼마나 오래 두나요?**
   - 왜 물어보나: 공모가 끝나면 지워야 한다면 "삭제" 버튼과 확인 절차가 필요하다. 계속 둔다면 삭제 기능을 안 만든다
   - 보기: (A) 계속 둔다 (B) 공모 끝나면 지운다 — 삭제 버튼 필요 (C) 선정된 지원서만 남기고 지운다
   - 답: ______

4. **"지원 받기"를 끄면 지원자 화면은 어떻게 보일까요?**
   - 왜 물어보나: 지원서 화면을 아예 숨길지, 문구를 띄울지에 따라 안내 화면 구조가 달라진다
   - 보기: (A) "지원하기" 버튼이 사라지고 "마감되었습니다" 문구가 보인다 (B) 지원서 화면까지는 가는데 제출 버튼만 막힌다 (C) 날짜(9월 30일)가 지나면 자동으로 꺼진다 + 스위치로도 끌 수 있다
   - 답: ______

5. **나 말고 어드민을 쓸 사람은 누구이고, 그 사람도 상태를 바꿀 수 있나요?**
   - 왜 물어보나: 비밀번호를 하나만 만들지 두 개(보기만 / 보고 바꾸기)를 만들지가 이걸로 정해진다. 권한 등급을 여러 개 만들지는 않는다. 최대 두 개
   - 보기: (A) 같이 운영하는 동료 1~2명. 나와 똑같이 상태를 바꿀 수 있다 — 비밀번호 하나 공유 (B) 심사위원. 보기만 하고 바꾸지는 못한다 — 보기 전용 비밀번호 하나 더 (C) 아직 모름 — 일단 비밀번호 하나로 시작
   - 답: ______

---

## 만들 때 이 순서로
1. `applications` 표에 `status` 칸 추가(기본값 "접수"), `settings` 표에 `is_open` 추가. RLS는 어드민 쪽에서만 읽고 쓸 수 있게
2. 잠금 화면 — 비밀번호 하나. 환경변수에 저장
3. 지원서 목록 화면 — 표 하나. 최신 순
4. 지원서 상세 화면 — 상태 바꾸기
5. "지원 받기" 스위치 — 끄면 지원자 화면이 질문 4의 답대로 바뀐다
6. (질문 2가 B면) 엑셀 내려받기 버튼
7. (질문 3이 B·C면) 삭제 버튼 + "정말 지울까요?" 확인

## 완성 기준
- [ ] 잠금을 통과해서 어드민에 들어간다
- [ ] 비밀번호 없이 어드민 주소를 열면 지원서가 보이지 않는다
- [ ] 지원서 목록이 최신 순으로 보인다
- [ ] 상태를 바꾸면 목록에 반영된다
- [ ] "지원 받기"를 끄면 지원자 화면이 정한 대로 바뀐다
- [ ] v1에서 되던 게 그대로 된다

## 넘길 때 쓰는 프롬프트
```
prd-v3-admin.md 를 읽고 어드민을 만들 건데, 바로 만들지 마.
문서 안의 "아직 정하지 못한 것" 을 나한테 하나씩 물어봐줘.
Ask Question 으로 내 답을 받아서 문서를 채운 다음에 만들자.
```
