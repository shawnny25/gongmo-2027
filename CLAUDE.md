# 작업 규칙

## 커밋은 클로드가 알아서
- 사용자가 "커밋해줘"라고 말하지 않아도, **의미 있는 작업 단위가 끝날 때마다** 로컬에 커밋한다. (기능 하나, 디자인 변경 하나, 설정 하나 등)
- 커밋 메시지는 한국어 한 줄로 무엇을 했는지. 예: `제목 타이핑 애니메이션 추가`
- `origin` 리모트가 연결돼 있으면 커밋 직후 `git push`까지 한다. push가 인증 때문에 실패하면 로컬 커밋은 그대로 두고 사용자에게 한 줄로 알린다.
- 커밋 전에 `git status`로 무엇이 들어가는지 확인한다. 비밀 값(서비스 키, 비밀번호)은 절대 커밋하지 않는다. Supabase publishable 키는 공개용이라 괜찮다.

## 프로젝트
- 정적 사이트 (HTML/CSS/JS) + Supabase. 빌드 없음. 설계도는 `prd-v1.md`, `prd-v2.md`, `prd-v3-admin.md`.
- 자주 바뀌는 글자는 `content.js` 한곳에 모은다.
- 배포는 Vercel. 로컬 미리보기: `python -m http.server 5173`
